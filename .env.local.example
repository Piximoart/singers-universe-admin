# Admin session cookie verification (must match backend signing key)
ADMIN_JWT_SECRET=<same secret as backend ADMIN_JWT_SECRET>

# Bridge to one backend owner
ADMIN_BACKEND_URL=https://singers-universe-backend.onrender.com
NEXT_PUBLIC_API_URL=https://singers-universe-backend.onrender.com
ADMIN_BRIDGE_TOKEN=<same secret as backend ADMIN_BRIDGE_TOKEN>
